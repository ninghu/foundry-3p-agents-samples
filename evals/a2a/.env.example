# --- Azure OpenAI configuration for evaluator runs (Task Adherence + Intent Resolution) ---
# Tip: after running `azd up`, execute `azd env refresh` and then
# `azd env get-value AZURE_OPENAI_ENDPOINT` (and DEPLOYMENT) to populate these values.
AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=your-deployment
AZURE_OPENAI_KEY=your-azure-openai-key

# --- Azure AI Project configuration (used to fetch the remote agent connection) ---
# You can pull the provisioned endpoint with `azd env get-value AZURE_AI_PROJECT_ENDPOINT`.
AZURE_AI_PROJECT_ENDPOINT=https://your-ai-foundry-endpoint/api/projects/your-project
